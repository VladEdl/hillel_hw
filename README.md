# HILLEL_HW
## Реалізуйте функцію removeElement
Реалізуйте функцію removeElement(array, item), щоб видалити елемент item з масиву array.

Наприклад:

    const array = [1, 2, 3, 4, 5, 6, 7];
    removeElement(array, 5 );
    console.log(array);
    // Результат: [1, 2, 3, 4, 6, 7]
Це дз можна виконати кількома способами:

Використати звичайні цикли, функції і умови котрі ми проходили на минулих уроках.
Без якихось новиї та специфічних знань.

Використати методи масивів: splice, indexOf
